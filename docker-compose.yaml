version: "3.7"
services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    depends_on:
      - xray
    networks:
      - xraynetwork
    command:
      - "--log-level"
      - "debug"
    ports:
      - "5000:5000"

  xray:
    container_name: xray
    build:
      context: .
      dockerfile: docker/xray/Dockerfile
    environment:
      AWS_REGION: ap-northeast-1
      AWS_PROFILE: "ta-da"
    ports:
      - "2000:2000/udp"
    command:
      - "-o"
    networks:
      - xraynetwork
    volumes:
      - "~/.aws/:/root/.aws/:ro"

networks:
  xraynetwork: